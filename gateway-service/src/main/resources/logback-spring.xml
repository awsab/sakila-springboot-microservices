<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!--
        Console appender — human-readable output for local development.
        Includes trace/span IDs (populated by OTel auto-instrumentation) and
        custom MDC fields set by RequestContextEnrichmentFilter.
    -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg [traceId=%X{trace_id:-} spanId=%X{span_id:-} requestId=%X{requestId:-} userId=%X{userId:-} tenantId=%X{tenantId:-}]%n</pattern>
        </encoder>
    </appender>

    <!--
        OpenTelemetry Logback Appender — bridges every Logback LoggingEvent into the
        OTel Logs pipeline so logs are exported via OTLP alongside traces and metrics.

        captureExperimentalAttributes: thread.name, logger.name
        captureCodeAttributes:         code.namespace / code.function / code.lineno
        captureMarkerAttribute:        SLF4J Marker forwarded as log record attribute
        captureMdcAttributes:          custom MDC keys forwarded as log record attributes
    -->
    <appender name="OpenTelemetry"
              class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">
        <captureExperimentalAttributes>true</captureExperimentalAttributes>
        <captureCodeAttributes>true</captureCodeAttributes>
        <captureMarkerAttribute>true</captureMarkerAttribute>
        <captureMdcAttributes>requestId,userId,tenantId</captureMdcAttributes>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT" />
        <appender-ref ref="OpenTelemetry" />
    </root>

</configuration>
